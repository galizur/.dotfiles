#!/bin/sh
#
# ~/.config/sh/environ - sh/bash global environment

# Load XDG specifications.
[ -f ~/.config/sh/xdg ] && . ~/.config/sh/xdg

# Fix issues with Java applications and Wayland.
export _JAVA_AWT_WM_NONREPARENTING=1

# Use qutebrowser as default browser.
export BROWSER="firefox"
export MOZ_ENABLE_WAYLAND=1

# Tells Ccache to only use compilers in this directory.
export CCACHE_PATH="/usr/bin"

# Use emacsclient as default text editor.
export EDITOR="emacsclient -a '' -t"

# Set identity and locale.
export EMAIL="karolos.triantafyllou@gmail.com"
export NAME="Karolos Triantafyllou"
export TZ="Europe/Athens"

# Used by FZF
export FZF_DEFAULT_OPTS='--layout=reverse --border'

# Identify the private key.
export GPGKEY="$(gpg -K | awk 'NR==4 {print $1}' | sed 's/4096R\///g')"

# Used by gpg-agent.
export GPG_TTY="$(tty)"

# Don't save lines which begin with a <space> character and eliminate duplication.
export HISTCONTROL="ignorespace:erasedups"
# Number of lines to store in an active session.
export HISTFILESIZE=100000
# Ignore the archiving of the following commands.
HISTIGNORE_BASE="[bf]g:cd:cd ~:cd ..:..:date:exit:halt:* --help:* help:history"
HISTIGNORE_BASE="$HISTIGNORE_BASE:jobs:l:ls:ls -?:ls -??::pwd:reboot:shutdown"
# Ignore common local commands.
HISTIGNORE_LOCAL="o:oo"
# Export combined HISTIGNORE.
export HISTIGNORE=${HISTIGNORE}:${HISTIGNORE_BASE}:${HISTIGNORE_LOCAL}
# Number of lines to store in history file after session end.
export HISTSIZE=$HISTFILESIZE

# Related to Java.
export JAVA_HOME="/usr/lib/jvm/default-runtime"
export JDK_HOME="/usr/lib/jvm/default"

# Enable code syntax coloring in `less`.
export LESS='-R '
export LESSOPEN="| /usr/bin/source-highlight-esc.sh %s"

# Disable warning for GTK-based applications.
export NO_AT_BRIDGE=1

# Set length of passwords generated by pass.
export PASSWORD_STORE_GENERATED_LENGTH=50

# Allows you to use $HOME/.local/bin's bash scripts anywhere.
export PATH="$PATH:$HOME/.local/bin"

# Allows Qt 5 to enable Wayland support.
export QT_QPA_PLATFORM=wayland
export QT_WAYLAND_DISABLE_WINDOWDECORATION="1"
export QT_WAYLAND_FORCE_DPI=physical

# Elementary
export ECORE_EVAS_ENGINE=wayland_egl
export ELM_ENGINE=wayland_egl

# SDL
export SDL_VIDEODRIVER=wayland

# Use evince as default document reader.
export READER="evince"

# Increase the history save size.
export SAVEHIST=10000

# Use foot as default terminal.
export TERMINAL="foot"

# Add temporary files according to the user.
export TMPDIR="/tmp/$USER"
if [ ! -d "$TMPDIR" ]; then
    mkdir -m 700 "$TMPDIR"
fi

# nnn
export NNN_PLUG='f:finder;o:fzopen;p:mocplay;d:diffs;t:nmount;v:imgview;g:getplugs'

# AMD stuff
export AMD_VULKAN_ICD=RADV
export DISABLE_LAYER_AMD_SWITCHABLE_GRAPHICS_1=1

export LANG=en_US.UTF-8
