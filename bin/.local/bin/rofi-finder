#!/bin/bash
#
# Allows you to find local files
#
# NOTE: This script uses 'locate' instead of 'find' for better performance.
# Therefore, remember to update the database for mlocate with
# 'sudo updatedb -U /home/$USER'
#
# To speed up the search, add your .cache and .local folders to PRUNEPATHS
# variable of the /etc/updatedb.conf file and update again

set -e

xdg-open "$(locate -q home | rofi -dmenu -no-custom -i -p "📁" -theme finder)"
