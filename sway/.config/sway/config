exec exec uwsm finalize SWAYSOCK I3SOCK XCURSOR_SIZE XCURSOR_THEME XDG_CURRENT_DESKTOP DISPLAY WAYLAND_DISPLAY
include configs/variables.conf
include configs/outputs.conf
include configs/idle.conf
include configs/inputs.conf
include configs/keybinds.conf
include configs/statusbar.conf

default_border pixel 2


set $gnome-schema org.gnome.desktop.interface

exec_always {
	gsettings set $gnome-schema gtk-theme 'Nordic'
	gsettings set $gnome-schema font-name 'Roboto 11'


#include /etc/sway/config.d/*

exec uwsm app -- "usr/lib/polkit-kde-authentication-agent-1"
exec uwsm app -- openrgb --startminimized -p main
exec uwsm app -- wl-paste --type text --watch cliphist store
exec uwsm app -- wl-paste --type image --watch cliphist store
exec uwsm app -- corectrl --minimize-systray
exec uwsm app -- udiskie -s
exec uwsm app -- polychromatic-tray-applet
#exec uwsm app -- mpd
